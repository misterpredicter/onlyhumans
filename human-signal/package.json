{
  "name": "onlyhumans",
  "private": true,
  "workspaces": [
    "web",
    "agent"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web"
  },
  "dependencies": {
    "@x402/fetch": "^2.8.0"
  }
}
